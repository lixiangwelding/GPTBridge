# V2 会话发起的 MCP 恢复输出修复

任务 d62104a7-3669-46ec-83b8-b63a01fc6b8b；本轮明确授权个人 MCP 修复、精确提交推送和保留配置安装，再回 V2。没有游戏生产部署、发奖、发信、SQL 写入或重启授权。

## 实际根因与边界

V2 两次补丁写入分别等待 5059/5006ms 后返回 RESOURCE_BUSY；同窗有共享 source 锁的构建。没有锁泄漏证据，不清锁、不取消他人作业。消费者查询没有精确的 exec 审计；checkpoint 引用请求 ID 不代表执行。本轮另一次 Git 日志读取被外层拦截，未绕过；MCP 代码修复不解除外层安全拒绝。

另发现 task_status(limit=5) 仍可能返回50条作业：task_view只限制steps，未限制Store job_list的返回。仅在 src-tauri/src/tools/personal.rs 修正输出边界并增加六个隔离回归。显式limit约束作业摘要，默认20，保留Store最大50及truncated。作业记录、身份、执行、步骤cursor、权限均不改。这里只减少输出，未声称减少底层最多50条读取。

## 测试与审查

- 原源码SHA：3ad4e3dab6a9c310be25044002282f780fe351dc8f9630b9051b3ac366e4ae99。
- 红例源码SHA：eef96be65243a66adc3fc2bfc5db5bfebcd2469bfc2a4d99eb7a805bb08ae32d。
- 修复源码SHA：e59fb91302711c53dcdd0f9bfc31ccdd8fa198c13677660fb2d2c28a634ffb6c。
- GitNexus task_view impact LOW，直接调用者open/status；.artifacts/checks/v2-mcp-d621-impact-1790075887051858000/。
- 六项回归旧实现3失败3通过，修复后6/6通过；.artifacts/checks/v2-mcp-d621-joblimit-red-1790076430136232000/ 与 v2-mcp-d621-joblimit-green-1790076547435347000/。
- 完整自测14/14阶段通过：.artifacts/selftest/1790076617683095000/result.json，前后源指纹eec2554fb86ce57089a7d3bd3f45abfd49c9912f94afe2539ec552cceafe1c49；回执SHA 3b1e8fcbb86b2e6f4f51a0c10a652d37e6d17028d3923f2d6def8c00b74f2121。定向与全套有重叠，不相加算唯一用例。
- code_review已实际读取文件与Plan，语义审查由本任务完成：保持上游截断标记，空任务不串号，不改变步骤cursor/作业数据库；git diff --check通过。指导工具exit0不等于自动语义审查通过。

## 并发与安装状态

原生补丁5秒锁等待未写入后，改用同一独占source锁保护的持久排队writer，执行前完整SHA守卫并原子替换，未把写入标作read。补丁共享预检/回执读取与白名单由2ecfafb2维护，PATH/执行预检及read_output身份由原写者维护，不重复覆盖其dirty。

本说明落盘时验证完成；精确提交、推送、安装与真实运行实例的最终结果记录在本task后继checkpoint。文件替换和运行激活分别核对，不能只看0.3.4版本号或HTTP200。V2三蛋维持disabled，游戏真实孵化与邮件未因此验收。
