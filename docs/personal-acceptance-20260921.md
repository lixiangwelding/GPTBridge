# 个人版真实验收记录 · 2026-09-21

## 本地源码与构建

- 个人版：0.3.0，独立普通仓库，不使用 worktree。
- 直接上游基线：`8e76bd06cce5519acad624cce209cd03a6f7bceb`。
- 最终自测命令：`python3 scripts/selftest_personal.py --suite all`。
- 本机私有回执：`.artifacts/selftest/1789987298891278000/result.json`。
- 源码指纹前后相同：`64c3327ecdfd33d2ba3ca5270f87d2e3898d0319760fe42f8075f2dcbbef505a`。
- 最终整体退出码：0。12 个阶段全部 passed。

| 阶段 | 实际结果 |
|---|---|
| Python 工具链与配置核验回归 | 退出 0 |
| Rust 持久任务运行时及真实 worker 子进程 | 退出 0 |
| 独立 worker 构建 | 退出 0 |
| MCP 实际 dispatcher 合同 | 退出 0 |
| MCP 协议兼容 | 退出 0 |
| 执行权限原有回归 | 退出 0 |
| 工具注册与描述 | 退出 0 |
| 完整 Rust 库回归 | **182 passed，0 failed，0 ignored** |
| 简化会话入口前端测试 | 退出 0 |
| Svelte 类型检查 | 退出 0 |
| 前端构建 | 退出 0 |
| macOS 桌面开发二进制构建 | 退出 0 |

定向 Rust 套件是完整库的子集，不重复累计成额外的业务验收数量。

## 与需求直接对应的覆盖

实际测试包含：同目录四个写入任务、同文件过期补丁拒绝与重读合并、补丁/命令/检查点幂等、千步分页与恢复、过期 checkpoint 拒绝、四命令实际并行、提交进程退出后的 worker 结果查询、超时、取消、stdin 关闭、输出上限、工作区隔离。测试中的步骤证据是夹具，不是用户业务项目已验收。

完整库回归曾发现 durable 输出缺少 `termination_reason` 导致 grep 无匹配误记审计失败；已保留原退出码，修复输出契约、截断标记和 queued 会话分类，并在最终完整回归中通过。中间的依赖/PATH及测试插入编译失败有独立失败回执，没有覆盖成成功。

## 配置继承

实际执行了 copy-only 导入，并运行 `python3 scripts/verify_personal_config.py` 退出 0。核验回执：`.artifacts/config-verification/1789987159461676000.json`。

结果：1 个工作区，个人端口 38766/38767；除文档明确的安全变换外，配置与本地凭据相同。关闭自动启动、隧道和上游代理，源配置只读。核验比较的是当时的当前源快照，且确认读取期间稳定；不伪造未记录的更早快照。

配置、凭据、SQLite、原始测试日志、依赖与构建缓存不提交 GitHub。公开验收文档仅保留不含秘密的结果摘要。

## 未进行的操作与边界

没有停止、重启、替换旧 MCP，没有改变其端口/隧道，没有切换现有连接器。新版未启动 GUI 或工作区监听；构建、测试和配置导入不等于手动 GUI、远程 OAuth 或生产环境验收。

没有 Windows 实机测试或签名安装包。并发锁只约束受管参与者，不能保证外部编辑器或旧 MCP 同时改文件时的完全隔离。未知 job 不自动重放，主机崩溃不承诺任意进程从指令级续跑；没有实现自动裁决未知外部副作用的接口。

辅助计划 CLI 的一次调用被平台拦截后停止，未通过其他入口绕过；本记录依据实际源码差异、编译、运行测试和回执，不宣称不可用的辅助图谱工具已经验收。
