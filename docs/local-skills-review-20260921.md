# 0.3.2 本地 Skill 桥接审查记录

## 最终复验更新

本次恢复后，最终 0.3.2 源码的完整自测已通过。回执 `.artifacts/selftest/1789994741430143000/result.json`，14 个阶段全部 passed、整体退出 0；完整 Rust 库 **235 passed / 0 failed / 0 ignored**，其中 Skill 专项 **28 passed**（是完整库子集，不重复累计）。测试前后源码指纹均为 `52d9c494251ded21bc962bd4507cf6edcf8a9b8d19aca25d4dde1cd89873bae3`。桌面开发二进制已重编译，不再使用旧审查前产物。

源码交付和宿主机安装分开记录：用户要求保留全部原配置并替换旧程序，但本轮用于定位实际旧程序和配置的只读盘点补丁被平台安全检查拦截，正式请求权限后原操作仍被拦截。没有改用其他机制绕过；**未完成旧程序/全部配置盘点、备份或本机替换**，不能将早期改端口和关闭隧道的隔离配置副本用于覆盖旧配置。旧服务、FRP 与原配置未被本轮修改。

## 上一轮阻塞记录（保留原始证据）

上一轮因执行通道故障未完成 commit/push，远端当时为 0.3.1 的 `e5c27a2d6b270f4602576cb3f92cdf6dc640407f`。以下旧回执说明当时状态；本次最终验收以上一节的新回执为准。

最后复验回执为 `.artifacts/selftest/1789992307598160000/result.json`，源码指纹 `52d9c494251ded21bc962bd4507cf6edcf8a9b8d19aca25d4dde1cd89873bae3`；读取时 Python 阶段 passed，runtime 阶段仍 running。原命令会话 `b72a675f-58a7-496b-969d-919bcd1d1505` 的查询和终止请求均返回 502，无法确认终态；没有重跑、强行发布或重启旧 MCP。恢复后先核原进程与回执，再运行匹配源码的完整自测，不把旧绿色回执绑定到新修改。

## 实现范围

本版增加原生 Rust 只读技能工具 `list_skills`、`search_skills`、`read_skill`、`invoke_skill`，复用个人版原有认证、工具分发、审计与多仓库路由。没有额外 Python/FastMCP 常驻服务，没有修改旧 MCP、FRP、Codex 配置或 Skill 原件。

`$技能名` 是消息发送后的模型工具调用约定，不会注册 ChatGPT 原生输入框下拉。出处为普通路径/行号/SHA 引用，不是平台原生 filecite。技能里声明脚本或 MCP 依赖不会使其自动执行或自动授权。

## 审查发现与修复

| 检查项 | 发现与处理 |
|---|---|
| 库内软链参考 | 初始版本只检查请求文件名，`guide.md` 可以在已授权库内指向凭据名称。真实合成测试复现失败后，对 canonical 目标再次验证文件名与类型。后继全库中该回归已通过。没有使用真实凭据复现。 |
| 引用一致性 | 搜索摘要与正文读取现使用相同 `skill://ID/库内相对路径`，避免引用路径前缀不同。对应回归已通过。 |
| YAML 普通描述 | 元数据中的普通 Markdown 强调不能误判为 YAML alias；增加回归并使用一次初始化的检测表达式。 |
| 旧异步测试时序 | 完整回归出现 `grep` 仍在运行时就断言最终审计结果的竞态。仅修改测试，轮询同一个 job，要求退出后再验证；不重发命令，不改生产执行结果，也不忽略错误。 |
| 目录与资源限制 | 只扫描当前仓库指定目录和明确全局目录，限制条目数、深度、元数据总量及单文件；截断和跳过结果显式标记。 |
| 多仓库隔离 | 工具调用继续要求 `workspace_id`，skill ID 带仓库作用域。真实本地 HTTP 覆盖未认证拒绝、搜索、加载；错误仓库不可复用 ID。 |
| 副作用与权限 | 所有技能工具只读，不启动脚本、外部 MCP 或网络请求；依赖执行仍需要原授权工具。 |

## 已取得的独立证据

- 首批原生 Skill 测试：25 项通过；随后新增描述、引用一致性和软链目标过滤回归。
- 第一次 0.3.2 完整 14 阶段通过，回执 `.artifacts/selftest/1789991710178467000/result.json`，源码指纹 `48540753bde9dd0527c8b08fda4cdc4c9093aa5e7b9f85cd5c6fec1d4e60b86e`。该回执早于最后审查修改，不能当作最终提交的完整通过证明。
- 本机原生只读检查：`.artifacts/checks/skills-native-local-1789991971225428000/result.json`，退出 0。实际发现 `frontend-development` 和兼容别名，3 个有效根、3 个匹配，未截断、跳过 0；没有启动服务或写配置。
- 软链目标合成失败：`.artifacts/checks/skills-review-red-1789992026805979000/result.json`。修复后该用例在后继全量中通过。
- 后继全量 `.artifacts/selftest/1789992131652048000/result.json` 为 234 通过、1 失败；失败来自上述旧 grep 测试过早判断 running 状态。失败回执保留，不改为成功。

代码发布使用本次新的完整回执及 Git 远端提交确认，不使用旧阶段回执替代。

## 未验证与适用边界

原生 ChatGPT `$` 下拉未实现；未进行真实用户聊天客户端实连、原生 GUI 人工验收、Windows 实机和签名安装包验收。Skills 的可选依赖/禁用策略没有自动完整导入 Codex 配置，不能冒称原生 Codex 全兼容。

Unix 使用逐组件 no-follow 普通文件打开；Windows 使用较弱路径检查，尚未进行实机竞态验证。文本内容本身可能含敏感数据或不可信指令，开放全局技能目录前仍应由拥有者审查；文件名过滤不是内容脱敏或操作系统沙箱。

辅助 code_insight 实际返回本地降级清单、无调用图，不能称完整 GitNexus 影响分析通过。本审查以真实改动、隔离测试和本机只读检查为依据。
