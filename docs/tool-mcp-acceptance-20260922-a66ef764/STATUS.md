> 后继更新：用户已授权修复/提交/推送/安装；代码6be33f2已推送，14/14回归通过，安装及实时接口已独立核验。最新事实见 [RELEASE.md](RELEASE.md) 与 results.json.latest。下方为前轮历史快照，不再代表当前未完成状态。

# TOOL-MCP 接续状态

任务：`a66ef764-4614-4053-9273-c4353fa060f8`。仅 Coding Tools MCP Personal；本目录由本任务写入。

用户快照：2026-09-22 19:27:39 +09:00。最近 Git 回执：2026-09-22 20:24:22 +09:00。本文件不是冻结源码，也不是完整业务验收证书。

## 本轮结论

已真实修复 `read_output` 丢失持久作业身份字段的问题，补充绑定、未绑定与 UTF-8 分页回归；修正两处 Python/venv 测试夹具。当前定向输出身份回归 1/1、路径回归 7/7 通过。真实客户端已验证补丁和命令幂等、陈旧 SHA 拒绝、中文 emoji 分页及独占无害作业的超时、取消。完整多会话、断线、最终合并构建与上线验收没有关闭。

本轮无 commit/push、无安装、无重启、无部署。不是再次安装 0.3.4 的任务。当前多写者修改及其他会话提交全部保留。

## 六状态逐项记录

| 项目 | source | tests | build | business / 真实运行 | git | deployment |
|---|---|---|---|---|---|---|
| TOOL-MCP-01 | 实际客户端 33 工具；保护补丁三字段真实可传；输出身份缺口已补源码 | 创建、更新、同请求重放、STALE_FILE、回读通过；输出身份定向 1/1 | 库测试已编译；未形成最终冻结桌面候选 | 合同局部通过；真实断连后恢复未验；运行实例的新输出字段未验 | 本轮未提交；继承及本轮修复仍需精确归属收口 | 未安装新修复，不宣称运行实例已包含它 |
| TOOL-MCP-02 | 复审原 17 代码/测试文件与 1 文档；本轮改 4 文件；后续多写者漂移保留 | 原全量 370通过/2失败；修复中 372通过/1失败；其后一次夹具编译失败；最终路径 7/7、身份 1/1 | runtime/worker 等阶段曾通过；全套未到最终桌面构建；没有同一最终 SHA 的完整通过 | 本地修复与定向回归成立，最终集成未验 | main 已由他会话前进至 71ba38e1；本轮提交/推送 0，未混提 | 旧安装保留；无本轮安装/配置更改 |
| TOOL-MCP-03 | 持久任务与 UTF-8 既有实现保留；输出页新增持久身份返回 | 真实命令去重、分页、超时、取消与任务查询通过；受控双 ChatGPT 竞争、断线及冷热长尾未验 | 使用当前测试库；未签发独立最终制品 | 真实其他会话并行和 RESOURCE_BUSY 已观察，但不是受控并发验收 PASS | 与 TOOL-MCP-02 同一待集成归属 | 本轮没有部署闭环 |
| TOOL-MCP-04 | 未修改 FRP/HTTP 运行逻辑 | 已取真实重名、超时与恢复日志；未定向重现唯一根因 | 无本项新构建 | 已证隧道层重名/重连链；旧进程、远端残留、502 与目录缓存的唯一因果未证 | 无本项提交 | 未重启，不以重启代替修复 |
| TOOL-MCP-05 | 文本 $ 别名与 Skill 工具可用；native_dollar_picker=false | 本轮 Skill 搜索、正文调用成功；Windows/移动端/全新 OAuth/签名/回退未验 | 无正式签名制品 | 不能宣传原生 $ 下拉；跨端及正式分发仍未验 | 无本项提交 | 不执行升级或回退 |

## 当前基线不是原快照

初始 `21c429ce0d95f5a553f78140dd9b0a2e19262696`；后继均由其他会话提交：

- `a76ca20f8d963289935f9e6b5fb5817888f2029e`：macOS GUI 工具链发现。
- `d97244bfd1779910f49bea1042e81936b82cae05`：可执行权限预检。
- `05700b30f11fcd374fca149dbdb63e00e14c9468`：已安装判断的输入漂移保护。
- `71ba38e10e731a6cea528870b5d6983448be9395`：补丁锁、回执重读和哈希诊断。

最近 git_status 为 main / origin/main，ahead=0、behind=0（本任务未 fetch）。这不等于本任务修复已提交。原 22 文件工作不重做。最终合并仍须确认 dispatch/exec 等共享文件写者、当前源码清单及唯一构建窗口。

## 安装身份和证据限制

本轮成功实读安装文件 SHA256：`dcc37a568d49d50eb87d902d73442da0d7c917e822a6a98ecf0417a604f065f0`，对应回执 job `41804f75-ded1-47e7-90ce-89e0a16ebad1`。它与会话87安装回执相同，不是早期未打包候选 `637c1367…0599ce`。

当前 PID 映射查询被白名单拒绝，正式权限申请后仍拒绝；最后批量 SHA 复核又被平台安全层拦截。未换通道绕过，因此不得把最后观测值描述为原子、实时的进程—连接—源码—制品证明。详情见 HANDOFF.md 与 results.json。
