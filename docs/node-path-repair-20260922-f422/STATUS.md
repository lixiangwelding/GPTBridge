# TOOL-MCP Node 执行修复与安装验收

任务：`f422e0ca-91c3-4b7c-a0a9-23c30b84df41`；日期：2026-09-22。

**本范围已完成修复、测试、代码提交推送、原位安装和真实连接器运行复验。**

## 实际修复

GUI PATH 缺少 Homebrew 工具目录，导致裸 Node 找不到、绝对 Node 被可执行身份校验拒绝。沿用同一 main 上经过审查的启动 PATH 补全；只识别已安装目录，不取消命令白名单或工作区边界。权限申请现在共用执行器预检，不会对缺失/越界程序误报 granted。

本任务补充五项隔离权限回归，修复集成中的 Git `Vec<Value>` 编译错误，以及根目录 list_dir 错把相对路径加上 `/` 的问题。目录复验复用已存在的四项专项用例。并发源码修改已核对合入，不回滚、不覆盖、不整仓暂存。

## 六状态

| 维度 | 结果 |
| --- | --- |
| 源码 | 指纹 `eec2554fb86ce57089a7d3bd3f45abfd49c9912f94afe2539ec552cceafe1c49` 与完整验收前后均一致。 |
| 测试 | 冻结输入完整 selftest 14/14 阶段通过；早期失败保留，重复套件不累加计数。 |
| 构建 | 前端、桌面、debug app bundle 成功；未宣称签名、公证通过。 |
| 实际运行 | 当前真实连接器 Node、绝对 Node、npm、npx、Cargo 可用；缺失程序正确 denied，未执行；根目录返回相对路径。最终 PID 66242 映射 inode 与安装文件一致，绝对 Node 再验成功。 |
| Git | 本任务三文件提交 `b29df973f44ee00024fee1b339142d76f1325c08` 已 push 并直接回读远端；其余已审修复由并发负责人分别整合。 |
| 安装 | 本轮正式安装并保持配置字节不变；其他会话随后安装并激活更晚同源制品，本任务保留新包并再次验证，不回盖旧包。 |

## 安装身份

路径：`/Users/didi/Applications/Coding Tools MCP Personal.app`；版本号仍为 0.3.4，按 SHA 区分修订。

本轮安装 SHA：`ecd76007ae5fcdfe4cf3b27b9e44610516c6719b2cd21969fc75f78c4af8b343`。

最终观察运行 SHA：`a9fbc18cad8ad723ef9a87b1a30405904dc18a56ca6997a5908621a3f19655c3`，PID 66242，端口 28766，安装与映射 inode 703129183。后继激活由其他并发会话完成，不冒称本任务独自重启。

本轮安装前后 profiles.json 字节一致。启动后的文件字节表示有变化，但与正式备份的 JSON 内容、鉴权项及全部密钥相同，changed_paths=[]。未回滚或改写配置。

桌面辅助功能只读查询曾被外层安全层拦截，未绕过，也未强杀旧实例。后续已观察到新进程并完成直接连接器复验。

## 验收边界

这里的 PASS 仅为个人 MCP 修复，不替代 MEDIA-VIDEO 原47项真实成片、人工听感、字幕语义或崩溃回收验收。无业务生产部署、公开投稿、发奖、DDL/RDS或云同步。

原始回执路径见 HANDOFF.md；详细状态与每项运行作业见 results.json。
